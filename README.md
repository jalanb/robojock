# robojock

A robot to create playlists

## Development

### Set up
```bash
cd /opt/clones/github/jalanb/robojock
rm -rf .venv
python3 -m venv --copies .venv
source .venv/bin/activate
python3 -m pip install .[develop]
```

### Claude

The Claude environment is stored in `docs/`
